import ChangePassword from './ChangePassword';
import Auth2FA from './Auth2FA';

const Security = () => {
  return (
    <div>
      <ChangePassword />
      <Auth2FA />
    </div>
  );
};

export default Security;

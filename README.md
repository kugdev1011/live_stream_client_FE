# Project Setup

### 1. Install dependencies

```
npm install
```

### 2. Create env file

Create `.env.local` file and copy content from `.env.example`. Update values if
needed.

### 3. Run project

```
npm run dev
```
